--- .github/workflows/ci.yml
+++ .github/workflows/ci.yml
@@ -677,10 +677,10 @@
   goreleaser:
     name: Run GoReleaser
     needs: [route, discover, release-context]
     if: |
       !failure() && !cancelled() &&
       needs.discover.outputs.has_goreleaser == 'true' &&
-      (needs.route.outputs.run_release == 'true' || needs.route.outputs.run_publisher == 'true')
+      (needs.route.outputs.run_publisher == 'true' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'release-test'))
     runs-on: ubuntu-latest
     permissions:
       contents: write
