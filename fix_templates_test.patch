--- cmd/g2/templates_test.go
+++ cmd/g2/templates_test.go
@@ -21,9 +21,10 @@
			var expectedContent []byte

			for _, file := range archive.Files {
-				if file.Name == "input.json" {
+				switch file.Name {
+				case "input.json":
					inputData = file.Data
-				} else if file.Name == "output.ebuild" {
+				case "output.ebuild":
					expectedContent = file.Data
				}
			}
